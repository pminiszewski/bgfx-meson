texturev_root = '../../../bgfx/tools/texturev/'

executable('texturev', [
  texturev_root + 'texturev.cpp',
  ],
  dependencies: [
    bx_dep,
    bimg_dep,
    bgfx_dep,
    examples_common_dep
  ]
)